import { Button } from '@mui/material'
import React from 'react'

const AddNew = () => {
  return (
    <div style={{
        marginTop: 20,
        textAlign: 'right'   
    }}>
        <Button variant='contained'>
            Add New
        </Button>
    </div>
  )
}

export default AddNew